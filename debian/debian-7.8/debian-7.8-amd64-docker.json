{
  "variables": {
    "iso_path": "Linux/debian/debian/dists/oldstable/main/installer-amd64/20130613+deb7u2+b4/images/netboot/mini.iso",
    "iso_checksum": "afa217cf0cf5a3e44ab02532e1d767f0",
    "vb_version": "4.3.24",
    "ssh_name": "root",
    "ssh_pass": "vagrant"
  },
  "builders": [
    {
      "type": "virtualbox-iso",
      "boot_wait": "10s",
      "disk_size": 512000,
      "guest_os_type": "Debian_64",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_checksum_type": "md5",
      "iso_urls": [
        "http://ftp.riken.jp/{{user `iso_path`}}"
      ],
      "ssh_username": "{{user `ssh_name`}}",
      "ssh_password": "{{user `ssh_pass`}}",
      "ssh_port": 22,
      "ssh_wait_timeout": "10000s",
      "shutdown_command": "echo '/sbin/halt -h -p' > shutdown.sh; echo 'vagrant' | sudo -S sh 'shutdown.sh'; rm -f shutdown.sh",
      "guest_additions_mode": "disable",
      "virtualbox_version_file": ".vbox_version",
      "vboxmanage": [
        [ "modifyvm", "{{.Name}}", "--memory", "512" ],
        [ "modifyvm", "{{.Name}}", "--cpus", "2" ],
        [ "modifyvm", "{{.Name}}", "--rtcuseutc", "on" ],
        [ "modifyvm", "{{.Name}}", "--vram", "32" ]
      ],
      "http_directory": "../http",
      "boot_command": [
        "<esc> <wait> install ",
        "preseed/url=http://{{ .HTTPIP }}:{{ .HTTPPort }}/preseed.cfg ",
        "debian-installer=en_US ",
        "auto ",
        "locale=en_US ",
        "kbd-chooser/method=us ",
        "keyboard-configuration/xkb-keymap=us ",
        "keyboard-configuration/layoutcode=us ",
        "netcfg/get_hostname={{ .Name }} ",
        "netcfg/get_domain=vagrantup.com ",
        "console-setup/ask_detect=false ",
        "console-keymaps-at/keymap=us ",
        "fb=false ",
        "debconf/frontend=noninteractive ",
        "hostname=vagrant <wait>",
        "<enter> <wait>"
      ]
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "scripts": [
        "../provisioners/base.sh",
        "../provisioners/vagrant.sh",
        "../provisioners/kernel-3.16.sh",
        "../provisioners/docker-io.sh",
        "../provisioners/virtualbox.sh",
        "../provisioners/cleanup.sh"
      ],
      "override": {
        "virtualbox-iso": {
        }
      },
      "environment_vars": [
        "VBOX_VER={{user `vb_version`}}"
      ]
    }
  ],
  "post-processors": [
    {
      "type": "vagrant",
      "output": "./debian-7.8-amd64-docker.box"
    }
  ]
}

