{
  "variables": {
    "variant": "xfce",
    "vm_name": "FreeBSD-13.1-RELEASE-amd64",
    "iso_image": "FreeBSD-13.1-RELEASE-amd64-disc1.iso",
    "path_to_iso": "releases/ISO-IMAGES/13.1",
    "iso_checksum": "sha256:697d81653fa246b921ddfcf1d15562c55249cc727b11fa3e47f470e2cf2b6a40",
    "box_version": "13.1.20220512",
    "num_cpus": "2",
    "mem_size": "1024",
    "disk_size": "51200",
    "remote_host": "{{env `REMOTE_HOST`}}",
    "remote_username": "{{env `REMOTE_USERNAME`}}",
    "remote_password": "{{env `REMOTE_PASSWORD`}}",
    "remote_datastore": "{{env `REMOTE_DATASTORE`}}",
    "root_password": "vagrant",
    "vagrant_username": "vagrant",
    "vagrant_password": "vagrant",
    "vagrant_group": "vagrant",
    "virtualbox_partition": "ada0",
    "vmware_partition": "da0",
    "qemu_partition": "vtbd0",
    "hyperv_partition": "da0",
    "virtualbox_netif": "em0",
    "vmware_netif": "em0",
    "qemu_netif": "vtnet0",
    "hyperv_netif": "hn0",
    "hyperv_switch_name": "Default Switch",
    "virtualbox_guest_os_type": "FreeBSD_64",
    "vmware_guest_os_type": "freebsd-64",
    "qemu_use_default_display": "true",
    "qemu_disaply": "",
    "DISTRIBUTIONS": "'base.txz kernel.txz lib32.txz'",
    "ABI": "FreeBSD:13:amd64",
    "headless": "false",
    "esxi_vnc_over_websocket": "true"
  },
  "builders": [
    {
      "type": "virtualbox-iso",
      "boot_wait": "10s",
      "cpus": "{{user `num_cpus`}}",
      "memory": "{{user `mem_size`}}",
      "disk_size": "{{user `disk_size`}}",
      "guest_os_type": "{{user `virtualbox_guest_os_type`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_urls": [
        "./iso/{{user `iso_image`}}",
        "https://download.freebsd.org/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "http://ftp.jp.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "http://ftp6.jp.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "http://ftp11.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "https://ftp4.tw.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}"
      ],
      "ssh_username": "root",
      "ssh_password": "{{user `root_password`}}",
      "ssh_timeout": "10000s",
      "shutdown_command": "echo '{{user `vagrant_password`}}' | sudo shutdown -p now",
      "guest_additions_mode": "disable",
      "http_directory": ".",
      "output_directory": "output/{{user `vm_name`}}-{{user `variant`}}-v{{user `box_version`}}-virtualbox",
      "vm_name": "{{user `vm_name`}}-{{user `variant`}}-v{{user `box_version`}}",
      "headless": "{{user `headless`}}",
      "vboxmanage": [
        [ "modifyvm", "{{.Name}}", "--rtcuseutc", "on" ],
        [ "modifyvm", "{{.Name}}", "--vram", "16" ]
      ],
      "boot_command": [
        "<enter>",
        "<wait10><wait10><wait10><wait5>",
        "s",
        "<wait5>",
        "dhclient {{user `virtualbox_netif`}}<enter><wait5>",
        "fetch -o /tmp/install_with_x11.sh http://{{ .HTTPIP }}:{{ .HTTPPort }}/install_with_x11.sh<enter>",
        "<wait>",
        "PARTITIONS={{user `virtualbox_partition`}} ",
        "DISTRIBUTIONS={{user `DISTRIBUTIONS`}} ",
        "BSDINSTALL_DISTSITE=/usr/freebsd-dist ",
        "ABI={{user `ABI`}} ",
        "ROOT_PASSWORD={{user `root_password`}} ",
        "VAGRANT_USER={{user `vagrant_username`}} ",
        "VAGRANT_PASSWORD={{user `vagrant_password`}} ",
        "VAGRANT_GROUP={{user `vagrant_group`}} ",
        "SUDO=sudo-1.9.10 ",
        "CA_ROOT_NSS=ca_root_nss-3.76 ",
        "NETIF={{user `virtualbox_netif`}} ",
        "bsdinstall script /tmp/install_with_x11.sh<enter>",
        "<wait10><wait10><wait10><wait10><wait10><wait10>"
      ]
    },
    {
      "type": "vmware-iso",
      "boot_wait": "10s",
      "cpus": "{{user `num_cpus`}}",
      "memory": "{{user `mem_size`}}",
      "disk_size": "{{user `disk_size`}}",
      "disk_type_id": "0",
      "guest_os_type": "{{user `vmware_guest_os_type`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_urls": [
        "./iso/{{user `iso_image`}}",
        "https://download.freebsd.org/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "http://ftp.jp.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "http://ftp6.jp.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "http://ftp11.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "https://ftp4.tw.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}"
      ],
      "ssh_username": "root",
      "ssh_password": "{{user `root_password`}}",
      "ssh_timeout": "10000s",
      "shutdown_command": "echo '{{user `vagrant_password`}}' | sudo shutdown -p now",
      "http_directory": ".",
      "output_directory": "output/{{user `vm_name`}}-{{user `variant`}}-v{{user `box_version`}}-vmware",
      "vm_name": "{{user `vm_name`}}-{{user `variant`}}-v{{user `box_version`}}",
      "headless": "{{user `headless`}}",
      "network": "nat",
      "network_adapter_type": "e1000",
      "vmx_data": {
        "ethernet0.present": "TRUE",
        "ethernet0.wakeOnPcktRcv": "FALSE",
        "ethernet0.addressType": "generated",
        "remotedisplay.vnc.enabled": "TRUE",
        "vhv.enable": "TRUE",
        "svga.vramSize": "16777216"
      },
      "boot_command": [
        "<enter>",
        "<wait10><wait10><wait10><wait5>",
        "s",
        "<wait5>",
        "dhclient {{user `vmware_netif`}}<enter><wait5>",
        "fetch -o /tmp/install_with_x11.sh http://{{ .HTTPIP }}:{{ .HTTPPort }}/install_with_x11.sh<enter>",
        "<wait>",
        "PARTITIONS={{user `vmware_partition`}} ",
        "DISTRIBUTIONS={{user `DISTRIBUTIONS`}} ",
        "BSDINSTALL_DISTSITE=/usr/freebsd-dist ",
        "ABI={{user `ABI`}} ",
        "ROOT_PASSWORD={{user `root_password`}} ",
        "VAGRANT_USER={{user `vagrant_username`}} ",
        "VAGRANT_PASSWORD={{user `vagrant_password`}} ",
        "VAGRANT_GROUP={{user `vagrant_group`}} ",
        "SUDO=sudo-1.9.10 ",
        "CA_ROOT_NSS=ca_root_nss-3.76 ",
        "NETIF={{user `vmware_netif`}} ",
        "bsdinstall script /tmp/install_with_x11.sh<enter>",
        "<wait10><wait10><wait10><wait10><wait10><wait10>"
      ]
    },
    {
      "type": "vmware-iso",
      "name": "esxi-iso",
      "boot_wait": "10s",
      "cpus": "{{user `num_cpus`}}",
      "memory": "{{user `mem_size`}}",
      "disk_size": "{{user `disk_size`}}",
      "disk_type_id": "thin",
      "guest_os_type": "{{user `vmware_guest_os_type`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_urls": [
        "./iso/{{user `iso_image`}}",
        "https://download.freebsd.org/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "http://ftp.jp.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "http://ftp6.jp.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "http://ftp11.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "https://ftp4.tw.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}"
      ],
      "ssh_username": "root",
      "ssh_password": "{{user `root_password`}}",
      "ssh_timeout": "10000s",
      "shutdown_command": "echo '{{user `vagrant_password`}}' | sudo shutdown -p now",
      "remote_type": "esx5",
      "remote_host": "{{user `remote_host`}}",
      "remote_username": "{{user `remote_username`}}",
      "remote_password": "{{user `remote_password`}}",
      "remote_datastore": "{{user `remote_datastore`}}",
      "vnc_over_websocket": "{{user `esxi_vnc_over_websocket`}}",
      "insecure_connection": true,
      "http_directory": ".",
      "output_directory": "{{user `vm_name`}}-{{user `variant`}}-v{{user `box_version`}}",
      "skip_export": true,
      "vm_name": "{{user `vm_name`}}-{{user `variant`}}-v{{user `box_version`}}",
      "headless": "{{user `headless`}}",
      "network": "bridged",
      "network_adapter_type": "e1000",
      "vmx_data": {
        "ethernet0.present": "TRUE",
        "ethernet0.networkName": "VM Network",
        "ethernet0.wakeOnPcktRcv": "FALSE",
        "ethernet0.addressType": "generated",
        "remotedisplay.vnc.enabled": "TRUE",
        "vhv.enable": "TRUE",
        "svga.vramSize": "16777216"
      },
      "boot_command": [
        "<enter>",
        "<wait10><wait10><wait10><wait5>",
        "s",
        "<wait5>",
        "dhclient {{user `vmware_netif`}}<enter><wait5>",
        "fetch -o /tmp/install_with_x11.sh http://{{ .HTTPIP }}:{{ .HTTPPort }}/install_with_x11.sh<enter>",
        "<wait>",
        "PARTITIONS={{user `vmware_partition`}} ",
        "DISTRIBUTIONS={{user `DISTRIBUTIONS`}} ",
        "BSDINSTALL_DISTSITE=/usr/freebsd-dist ",
        "ABI={{user `ABI`}} ",
        "ROOT_PASSWORD={{user `root_password`}} ",
        "VAGRANT_USER={{user `vagrant_username`}} ",
        "VAGRANT_PASSWORD={{user `vagrant_password`}} ",
        "VAGRANT_GROUP={{user `vagrant_group`}} ",
        "SUDO=sudo-1.9.10 ",
        "CA_ROOT_NSS=ca_root_nss-3.76 ",
        "NETIF={{user `vmware_netif`}} ",
        "bsdinstall script /tmp/install_with_x11.sh<enter>",
        "<wait10><wait10><wait10><wait10><wait10><wait10>"
      ]
    },
    {
      "type": "qemu",
      "boot_wait": "10s",
      "cpus": "{{user `num_cpus`}}",
      "memory": "{{user `mem_size`}}",
      "disk_size": "{{user `disk_size`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_urls": [
        "./iso/{{user `iso_image`}}",
        "https://download.freebsd.org/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "http://ftp.jp.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "http://ftp6.jp.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "http://ftp11.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "https://ftp4.tw.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}"
      ],
      "ssh_username": "root",
      "ssh_password": "{{user `root_password`}}",
      "ssh_wait_timeout": "10000s",
      "shutdown_command": "echo '{{user `vagrant_password`}}' | sudo shutdown -p now",
      "format": "qcow2",
      "accelerator": "kvm",
      "net_device": "virtio-net",
      "disk_interface": "virtio",
      "http_directory": ".",
      "output_directory": "output/{{user `vm_name`}}-{{user `variant`}}-v{{user `box_version`}}-qemu",
      "vm_name": "{{user `vm_name`}}-{{user `variant`}}-v{{user `box_version`}}",
      "headless": "{{user `headless`}}",
      "disk_compression": true,
      "use_default_display": "{{user `qemu_use_default_display`}}",
      "display": "{{user `qemu_display`}}",
      "boot_command": [
        "<enter>",
        "<wait10><wait10><wait10><wait10><wait10><wait10>",
        "<wait10><wait10><wait10>",
        "s",
        "<wait5>",
        "dhclient {{user `qemu_netif`}}<enter><wait5>",
        "fetch -o /tmp/install_with_x11.sh http://{{ .HTTPIP }}:{{ .HTTPPort }}/install_with_x11.sh<enter>",
        "<wait>",
        "PARTITIONS={{user `qemu_partition`}} ",
        "DISTRIBUTIONS={{user `DISTRIBUTIONS`}} ",
        "BSDINSTALL_DISTSITE=/usr/freebsd-dist ",
        "ABI={{user `ABI`}} ",
        "ROOT_PASSWORD={{user `root_password`}} ",
        "VAGRANT_USER={{user `vagrant_username`}} ",
        "VAGRANT_PASSWORD={{user `vagrant_password`}} ",
        "VAGRANT_GROUP={{user `vagrant_group`}} ",
        "SUDO=sudo-1.9.10 ",
        "CA_ROOT_NSS=ca_root_nss-3.76 ",
        "NETIF={{user `qemu_netif`}} ",
        "bsdinstall script /tmp/install_with_x11.sh<enter>",
        "<wait10><wait10><wait10><wait10><wait10><wait10>"
      ]
    },
    {
      "type": "hyperv-iso",
      "boot_wait": "10s",
      "cpus": "{{user `num_cpus`}}",
      "memory": "{{user `mem_size`}}",
      "disk_size": "{{user `disk_size`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_urls": [
        "./iso/{{user `iso_image`}}",
        "https://download.freebsd.org/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "http://ftp.jp.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "http://ftp6.jp.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "http://ftp11.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}",
        "https://ftp4.tw.freebsd.org/pub/FreeBSD/{{user `path_to_iso`}}/{{user `iso_image`}}"
      ],
      "ssh_username": "root",
      "ssh_password": "{{user `root_password`}}",
      "ssh_wait_timeout": "10000s",
      "shutdown_command": "echo '{{user `vagrant_password`}}' | sudo shutdown -p now",
      "http_directory": ".",
      "output_directory": "output/{{user `vm_name`}}-{{user `variant`}}-v{{user `box_version`}}-hyperv",
      "vm_name": "{{user `vm_name`}}-{{user `variant`}}-v{{user `box_version`}}",
      "headless": "{{user `headless`}}",
      "switch_name": "{{user `hyperv_switch_name`}}",
      "boot_command": [
        "<enter>",
        "<wait10><wait10><wait10><wait10>",
        "s",
        "<wait5>",
        "dhclient {{user `hyperv_netif`}}<enter><wait5>",
        "fetch -o /tmp/install_with_x11.sh http://{{ .HTTPIP }}:{{ .HTTPPort }}/install_with_x11.sh<enter>",
        "<wait>",
        "PARTITIONS={{user `hyperv_partition`}} ",
        "DISTRIBUTIONS={{user `DISTRIBUTIONS`}} ",
        "BSDINSTALL_DISTSITE=/usr/freebsd-dist ",
        "ABI={{user `ABI`}} ",
        "ROOT_PASSWORD={{user `root_password`}} ",
        "VAGRANT_USER={{user `vagrant_username`}} ",
        "VAGRANT_PASSWORD={{user `vagrant_password`}} ",
        "VAGRANT_GROUP={{user `vagrant_group`}} ",
        "SUDO=sudo-1.9.10 ",
        "CA_ROOT_NSS=ca_root_nss-3.76 ",
        "NETIF={{user `hyperv_netif`}} ",
        "bsdinstall script /tmp/install_with_x11.sh<enter>",
        "<wait10><wait10><wait10><wait10><wait10><wait10>"
      ]
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "scripts": [
        "../provisioners/vagrant-11.1+.sh",
        "../provisioners/xorg.sh",
        "../provisioners/xfce.sh",
        "../provisioners/slim.sh",
        "../provisioners/xrdp.sh"
      ],
      "execute_command": "chmod +x {{ .Path }}; env {{ .Vars }} {{ .Path }}",
      "environment_vars": [
        "ROOT_PASSWORD={{user `root_password`}}",
        "VAGRANT_USER={{user `vagrant_username`}}",
        "VAGRANT_PASSWORD={{user `vagrant_password`}}",
        "VAGRANT_GROUP={{user `vagrant_group`}}",
        "XFCE=xfce-4.16",
        "XORG_MINIMAL=xorg-minimal-7.5.2_3",
        "XTERM=xterm-372",
        "XCLOCK=xclock-1.0.9",
        "TWM=twm-1.0.11_1",
        "XRANDR=xrandr-1.5.1",
        "SLIM=slim-1.3.6_22",
        "SLIM_THEMES=slim-themes-1.0.1_2",
        "ARANDR=arandr-0.1.10_1",
        "XRDP=xrdp-0.9.19,1"
      ]
    },
    {
      "type": "shell",
      "only": ["virtualbox-iso"],
      "script": "../provisioners/virtualbox_ose_additions-10.4+.sh",
      "execute_command": "chmod +x {{ .Path }}; env {{ .Vars }} {{ .Path }}",
      "environment_vars": [
        "VIRTUALBOX_OSE_ADDITIONS=virtualbox-ose-additions-6.1.32"
      ]
    },
    {
      "type": "shell",
      "only": ["vmware-iso", "esxi-iso"],
      "script": "../provisioners/vmware-guest.sh",
      "execute_command": "chmod +x {{ .Path }}; env {{ .Vars }} {{ .Path }}",
      "environment_vars": [
        "VMWARE_WITH_XORG=1",
        "OPEN_VM_TOOLS=open-vm-tools-11.3.5_3,2",
        "XF86_VIDEO_VMWARE=xf86-video-vmware-13.3.0_6",
        "XF86_INPUT_VMMOUSE=xf86-input-vmmouse-13.1.0_5"
      ]
    },
    {
      "type": "shell",
      "only": ["virtualbox-iso", "vmware-iso", "qemu", "hyperv-iso"],
      "script": "../provisioners/disk_cleanup.sh",
      "execute_command": "chmod +x {{ .Path }}; env {{ .Vars }} {{ .Path }}"
    },
    {
      "type": "shell",
      "script": "../provisioners/cleanup.sh",
      "execute_command": "chmod +x {{ .Path }}; env {{ .Vars }} {{ .Path }}"
    }
  ],
  "post-processors": [
    {
      "type": "vagrant",
      "only": ["virtualbox-iso", "hyperv-iso"],
      "compression_level": 9,
      "output": "./{{user `vm_name`}}-{{user `variant`}}-v{{user `box_version`}}-{{ .Provider }}.box",
      "vagrantfile_template": "../vagrantfiles/Vagrantfile.FreeBSD-11+"
    },
    {
      "type": "vagrant",
      "only": ["vmware-iso", "qemu"],
      "compression_level": 9,
      "output": "./{{user `vm_name`}}-{{user `variant`}}-v{{user `box_version`}}-{{ .Provider }}.box",
      "vagrantfile_template": "../vagrantfiles/Vagrantfile.FreeBSD-11+",
      "keep_input_artifact": true
    }
  ]
}
